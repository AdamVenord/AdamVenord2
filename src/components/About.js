import React from "react"
import { Container } from 'semantic-ui-react'
import styles from '../css/mystyle.module.css'; 

const About = () => (
  // still not entirely sure what to put in here, definetly talk about your passions
  // add a few photos you and your friends, if they are okay with it (do it anyway hehe)
   

  <Container className={styles.home} class="center" >
    <h2>About</h2>
  </Container>
);

export default About;