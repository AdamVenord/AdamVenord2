import React from "react"
import { Container } from 'semantic-ui-react'
import styles from '../css/mystyle.module.css'; 

const Resume = () => (
// make it similar to the one I have now, maybe also look at that one website to make it better
// see if you can make it auto update directly on the website instead of having a link

  <Container className={styles.home} class="center" >
    <h2>Resume</h2>
  </Container>
);

export default Resume;